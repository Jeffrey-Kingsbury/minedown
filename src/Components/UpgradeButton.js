import { styled } from "styled-components";

const UpgradeButton = ({ title, description }) => {

    return (
<Wrapper>

</Wrapper>
    );
}

const Wrapper = styled.div`
`;


export default UpgradeButton;

